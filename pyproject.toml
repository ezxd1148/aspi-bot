[project]
name = "aspi-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.44.0",
    "python-dotenv>=1.0.0",
    "python-telegram-bot[job-queue]>=21.0",
    "requests>=2.34.2",
]
